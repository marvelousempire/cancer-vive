# Wire door

Law: intent ticket → Bootstrap English Manifest → micro-slice → Python → receipt.
A paragraph is not a wire.

01. Board names object and form. Architecture of Truth `01_truth_filter.py` returns lane and grade.
02. Board names the current egg. UNA law: G1 is open. G2–G5 stay closed until G1 has a receipt.
03. Board lists what a G1 receipt is and what is still missing. Biopsy is not G2. G1 receipt is still false.
04. Board names the two applications and this board as subject clay. G1 open, G2 closed.
05. Board locks G2 closed. Yolks empty. G1 receipt still false.
06. Board grades one Watch card by factor, form, and lane.
07. Board checks UNA one-egg law. Asked egg G3 returns hatch=false.
08. Smoke test checks the live wire functions without claiming G1, Embassy, I Speak, or GUI.
09. Window-close ticket defines safe, secure, confident without inventing care or receipts.
10. Exponential-backoff slice computes waits for refused calls without hammering a remote door.
11. Prescription research variable router resolves W-019 ivermectin and W-020 hydroxychloroquine/HCQ, calls Slice 06 for evidence grading, and returns a receipt with no treatment or dose authority.

## Slice 11 commands

```bash
python3 literature/wire/11_prescription_variable_router.py ivermectin
python3 literature/wire/11_prescription_variable_router.py hydroxychloride
python3 literature/wire/11_prescription_variable_router.py HCQ --lane treatment-claim
```

The preserved operator alias `hydroxychloride` resolves only to W-020 hydroxychloroquine. It does not create a general chemical alias rule.
