# Wire door

Law: intent ticket → Bootstrap English Manifest → micro-slice → Python → receipt.
A paragraph is not a wire.

01. Board names object and form. Architecture of Truth `01_truth_filter.py` returns lane and grade.
02. Board names the current egg. UNA law: G1 is open. G3 and G4 stay closed until G2 has a receipt.
